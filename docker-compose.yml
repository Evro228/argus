version: '3.8'

services:
  argus:
    build: .
    container_name: argus_cockpit
    restart: unless-stopped
    ports:
      - "127.0.0.1:8800:8800"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./uploads:/app/uploads
      - ./reports:/app/reports
